// import { Ingredient } from '../shared/ingredient.model';

export class SumInsured {
  public id: number;
  public name: string;
  public ratio: number;


  constructor(id: number, name: string, ratio: number) {

    this.id = id;
    this.name = name;
    this.ratio = ratio;

  }
}
